#include "lists.h"

int is_palindrome(listint_t **head)
{
    int i;
    listint_t *current, *rev;

    current = *head;
    if (head == NULL)
	return (1);

    for (i = 0; head->next != NULL; i++)
    {
	rev = *head;
    }
}
